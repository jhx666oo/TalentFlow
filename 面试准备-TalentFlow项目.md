# TalentFlow 面试准备指南

---

## 一、30 秒电梯演讲（项目介绍第一句）

> TalentFlow 是一个 **AI 驱动的全流程智能招聘管理平台**。我独立完成了前后端全栈开发，核心亮点是**用 LangGraph 构建了多 Agent 协作系统**——简历上传后自动 OCR 解析、AI 五维度评分、邮件协商面试时间、钉钉同步日程，实现了从投递到入职的全链路自动化。

---

## 二、面试官最可能问的 10 个问题 + 回答思路

### Q1：说一下这个项目的整体架构？

**答：** 前后端分离。前端 Vue 3 + Element Plus + ECharts 数据看板，部署在 Vercel。后端 FastAPI 异步架构，部署在阿里云 ECS，用 Docker Compose 编排了 PostgreSQL、Redis、Gunicorn + Uvicorn、Caddy 自动 SSL 四个服务。

分层结构：router（接口） → repository（数据访问） → model（ORM），schemas 做类型校验，tasks 做异步任务，agents 做 AI 能力。

**追问加分点：** 为什么用异步 FastAPI 而不是 Flask？——因为 OCR 解析、AI Agent 调用都是 IO 密集型，异步不阻塞主线程，吞吐量更高。

---

### Q2：AI Agent 是怎么设计的？

**答：** 用了 **LangGraph + LangChain**。核心是两层 Agent：

1. **简历解析 Agent**（`resume.py`）——单次调用，OCR 文本进来，结构化 JSON 出去，用 qwen3-max，`response_format` 强制结构化输出。

2. **候选人流程 Agent**（`candidate.py`）——这是一个多轮对话 Agent，有 6 个 Tool：评分、查面试官钉钉空闲时间、发邮件、确认面试、拒绝面试、获取当前时间。整个流程在 LangGraph 状态图中自动编排。

**关键设计：**
- 状态用 PostgreSQL Checkpointer 持久化——面试官隔天回复邮件，Agent 能接上之前的对话
- 对话超 50000 token 自动摘要压缩（`SummarizationMiddleware`）
- 主模型挂了自动切备选（`ModelFallbackMiddleware`）

---

### Q3：OCR 解析怎么做？为什么用双引擎？

**答：** 双引擎降级策略：

- **主引擎 PaddleOCR**——公有云 API，异步轮询模式（创建 job → 每 2 秒轮询 → 获取结果），支持 PDF/Word/图片
- **备引擎 Qwen-VL-OCR**——当 PaddleOCR 失败时自动降级，用大模型视觉能力直接识别。PDF 先转图片再 Base64 编码发送

**设计原因：** PaddleOCR 专门做 OCR，准确率和速度更好但偶有服务不稳定；Qwen VL 作为兜底保证可用性。

**追问：** 为什么不直接用 Qwen VL？——PaddleOCR 对复杂排版（表格、多栏）的识别效果更好，而且成本更低。

---

### Q4：候选人状态机怎么设计的？

**答：** 9 种状态，**严格单向流转**：

```
已投递 → AI筛选成功/失败 → 待面试 → 拒绝面试/面试通过/未通过 → 已入职/已拒绝
```

在代码里用一个状态列表，每次更新时检查 `新状态索引 > 旧状态索引`，不允许回退。AI 评分 ≥ 8 分自动标记为通过，< 8 分自动淘汰，但支持人工豁免。

---

### Q5：权限系统怎么做的？

**答：** JWT 双 Token 机制。Access Token 7 天，Refresh Token 30 天。Access Token 过期返回 403（允许用 Refresh Token 刷新），Refresh Token 过期返回 401（需要重新登录）。

角色分三级：超管（看全部）、HR（看自己管理部门下的候选人，通过 JOIN position 的 department_id 过滤）、面试官（只看自己发布的职位）。

**追问：** 为什么 Access Token 过期是 403 而不是 401？——语义区分：401 是真的没登录，403 是登录了但 token 过期，前端根据不同状态码做不同处理（刷新 vs 跳转登录）。

---

### Q6：邮件协商面试时间是怎么实现的？

**答：** 这是一个异步协作场景。有一个独立的调度器进程（APScheduler），每 15 秒通过 IMAP 轮询收件箱，按发件人邮箱匹配候选人，把邮件内容传给 Agent 处理。

Agent 理解邮件内容后：候选人说"确定"→ 直接确认；说"换个时间"→ 调钉钉 API 查面试官空闲时间 → 回复候选人；说"不来了"→ 更新状态为拒绝。

同时用钉钉 API 创建日历事件，面试官在钉钉里能看到日程。

---

### Q7：遇到什么技术难点？怎么解决的？

**答：** 讲一个具体的，比如：

1. **多轮对话状态持久化**——最开始用内存存储，重启就丢。后来改成 PostgreSQL Checkpointer，状态序列化到数据库，服务重启也能恢复。但带来了新的问题：对话越来越长，token 消耗爆炸。加了 `SummarizationMiddleware`，超过 50000 token 自动摘要压缩到 10000 token。

2. **Gunicorn 多 Worker 下定时任务重复执行**——APScheduler 在每个 Worker 里都会跑，导致邮件被重复处理。解决方案：起一个独立进程专门跑调度器，不和 API 服务混在一起。

---

### Q8：数据库连接池怎么配置的？

**答：** `pool_size=10, max_overflow=20`，关键的两个配置：

- `pool_pre_ping=True`：每次拿连接前先 ping 一下，防止拿到已断开的连接（比如数据库重启、网络闪断）
- `pool_recycle=3600`：每小时强制回收连接，防止内存泄漏

---

### Q9：为什么选 FastAPI 而不是 Django？

**答：** 三个原因：
1. FastAPI 原生异步，这个项目有大量 IO 操作（OCR API 调用、AI Agent、邮件、钉钉 API），异步不阻塞
2. Pydantic 类型校验天然适合做 API 参数验证
3. 自动生成 Swagger 文档，联调方便

Django 太重了，这个项目不需要 admin 后台和模板引擎。

---

### Q10：线上部署怎么做的？

**答：** Docker Compose 一键部署。四个容器：PostgreSQL 16、Redis 7、Backend（Gunicorn + Uvicorn worker）、Caddy（反向代理 + 自动 Let's Encrypt SSL）。

前端部署在 Vercel（CDN 加速），API 部署在阿里云 ECS。Caddy 最大的好处是自动申请和续期 SSL 证书，不需要手动配 Nginx + Certbot。

---

## 三、技术亮点清单（引导面试官追问的点）

面试时有意提到这些点，面试官大概率会追问，你就有准备地展开：

| 亮点 | 一句话 | 引导语 |
|------|--------|--------|
| **双引擎降级** | OCR 主备引擎 + LLM 主备模型 | "为了保证可用性，我做了双层降级..." |
| **LangGraph 状态图** | 多 Agent 协作 + 对话持久化 | "AI Agent 这块比较有意思，用了 LangGraph..." |
| **异步邮件协商** | IMAP 轮询 + Agent 理解回复 | "面试时间协商是异步的，候选人回复邮件后 Agent 会自动处理..." |
| **Connection Pool 优化** | pool_pre_ping + pool_recycle | "数据库连接池这块我特别注意了..." |
| **独立调度器进程** | 避免 Gunicorn 多 Worker 重复调度 | "部署时发现一个坑，定时任务在多个 Worker 里重复执行..." |
| **Token 缓存设计** | 前缀隔离 + 自动刷新 | "Redis 缓存我做了一个前缀隔离的设计..." |

---

## 四、项目演示流程（如果让现场演示）

**建议路线（5-10 分钟）：**

1. **登录页** → 用 boss@qq.com / 111111 登录，展示角色权限
2. **数据看板** → 展示最近 7 天候选人趋势图（ECharts）
3. **上传简历** → 上传一个 PDF 简历，展示 OCR 解析结果，强调 AI 自动填充
4. **候选人列表** → 展示 AI 评分结果（5 个维度 + 优劣势分析），强调 ≥8 分自动通过
5. **状态流转** → 把候选人从"AI筛选成功" → "待面试" → "面试通过"走一遍
6. **员工管理 / HR 管理** → 展示多角色权限

**提前准备：**
- 确保线上环境能正常访问
- 准备一份测试简历 PDF
- 如果有录屏需求，建议本地跑一遍更稳定

---

## 五、面试话术技巧

### 第一印象

不要说："这是一个练手项目/课程项目"

要说："这是我独立设计开发的全栈项目，从架构设计到线上部署都是我做的，目前在线上正常运行。"

### 遇到不会的问题

不要说："这个我不太清楚"

要说："这个我在开发中确实没深入考虑过，但以我现在的理解，如果要做的话，我会考虑用 XXX 方案。您觉得这个方向对吗？"（展示思考能力）

### 被质疑时

不要说："嗯对这里确实写的不好"

要说："这里确实有改进空间，我当时的选择是基于 XXX 考虑。如果现在重新做，我会考虑 XXX。"（展示复盘能力）

---

## 六、面试前检查清单

- [ ] 线上环境能否正常访问（www.ai-bot.icu）
- [ ] API 服务是否正常运行（api.ai-bot.icu/docs）
- [ ] 测试账号能否登录成功
- [ ] 准备一份简历 PDF 用于现场演示
- [ ] 本地代码能跑起来（备用）
- [ ] 能画出核心架构图（纸笔 or 口述）
- [ ] 把上面 10 个问题的回答在心里过一遍
- [ ] 想好一个技术难点的故事（有背景、有过程、有结果）
- [ ] 准备好反问面试官的问题
